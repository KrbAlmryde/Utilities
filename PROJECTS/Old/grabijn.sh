. $PROFILE/${1}_profile.sh
cd ${func_dir}/${run}
####################################################################################################
echo "working"

echo "${func_dir}/${run}"

cp ${runsub}.nii.gz ${ICA_dir}/${cond}/${run}

echo "${ICA_dir}/${cond}/${run}"
